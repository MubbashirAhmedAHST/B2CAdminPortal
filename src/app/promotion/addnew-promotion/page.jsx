import CreatePromotionSection from "./components/CreatePromotionSection";

export const metadata = {
  title: "Add New Promotion | Admin | Click Ship & Go",
  description: "Add New Promotion page",
};

export default function CreatePromotion() {
  return <CreatePromotionSection />;
}
