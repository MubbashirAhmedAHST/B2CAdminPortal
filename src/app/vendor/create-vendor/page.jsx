import CreateVendorSection from "./components/CreateVendorSection";

export const metadata = {
  title: "Add New Vendor | Admin | Click Ship & Go",
  description: "Add New Vendor page",
};

export default function CreateVendors() {
  return <CreateVendorSection />;
}
