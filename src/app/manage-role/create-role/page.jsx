import CreateRoleSection from "./components/CreateRoleSection";

export const metadata = {
  title: "Add New Role | Admin | Click Ship & Go",
  description: "Add New Role page",
};

export default function CreateRole() {
  return <CreateRoleSection />;
}
