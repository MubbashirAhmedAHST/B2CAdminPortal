import ListRoleSection from "./components/ListRoleSection";

export const metadata = {
  title: "Manage Roles | Admin | Click Ship & Go",
  description: "Manage Roles page",
};

export default function ListRole() {
  return <ListRoleSection />;
}
